# Database Configuration
# PostgreSQL (Production - Recommended)
DATABASE_URL="postgresql://username:password@localhost:5432/podsee?schema=public"

# SQLite (Development/Testing Only - NOT for production)
# DATABASE_URL="file:./dev.db"

# Node Environment
NODE_ENV="production"

# Application Port (optional, defaults to 3001)
PORT=3001

# Prisma Configuration (optional)
PRISMA_HIDE_UPDATE_MESSAGE="true"

# NextAuth Configuration (if using authentication)
NEXTAUTH_URL="https://yourdomain.com"
NEXTAUTH_SECRET="generate-a-secure-random-string-here"

# Optional: Database Connection Pooling
# Add these parameters to DATABASE_URL for better performance:
# ?connection_limit=10&pool_timeout=20
